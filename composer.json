{
  "name": "ost/restrict-voucher",
  "license": "proprietary",
  "type": "shopware-plugin",
  "extra": {
    "installer-name": "OstRestrictVoucher"
  },
  "scripts": {
    "test": "phpunit -c tests/ --colors=always",
    "test-unit": "phpunit -c tests/phpunit_unit.xml --colors=always"
  }
}
